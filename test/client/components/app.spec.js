import Vue from 'vue';
import App from '../../../src/client/components/App';

describe('App' () => {
  it('has been created' () => {
    expect(typeof MyComponent.created).toBe('function');
  });
});
